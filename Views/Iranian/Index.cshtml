@model List<SoliAntique.Models.IranianCarpet>
@{
    ViewData["Title"] = "Iranian Carpets";
    Layout = "_Layout";
}

<h2>Iranian Carpets</h2>
<div class="row g-4">
@foreach (var carpet in Model)
{
    <div class="col-md-4">
        <a href="/Iranian/Details/@carpet.Id" class="text-decoration-none text-dark">
            <div class="card h-100 shadow-sm border-0">
                <div style="height: 360px; overflow: hidden;">
                    <img src="@carpet.Images.FirstOrDefault()?.ImageUrl" style="width:100%; height:100%; object-fit:contain;" />
                </div>
                <div class="card-body">
                    <h5 class="card-title">@carpet.Title</h5>
                    <p class="card-text">@carpet.Dimensions</p>
                    <p class="h5">$@carpet.Price</p>
                </div>
            </div>
        </a>
    </div>
}
</div>
